import { ConfigSearchPipe } from './config-search.pipe';

describe('ConfigSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfigSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
