import { AppsConfigPipe } from './apps-config.pipe';

describe('AppsConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new AppsConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
